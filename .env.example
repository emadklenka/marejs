# MareJS Example Environment Configuration

NODE_ENV=development
PORT=4000

# Use a long, cryptographically secure value for production!
SESSION_SECRET=your-super-secure-session-secret-key-here

# Comma-separated list of allowed origins for CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3002

# Set to true to allow cookies to be sent cross-origin (if needed)
CORS_CREDENTIALS=true

# Set to true in production to enable secure cookies (HTTPS only)
SECURE_COOKIES=false